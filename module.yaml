product:
  type: kmp/lib
  platforms: [ android ]

dependencies:
  - $compose.foundation
  - $compose.material3

repositories:
  - id: mavenLocal
    url: mavenLocal
    publish: true

settings:
  compose: enabled
  publishing:
    enabled: true
    version: 0.0.1
    group: "com.github.meowgit"
    artifactId: ui